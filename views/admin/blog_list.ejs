<%- include('header.ejs'); %>


    <!-- ===== DASHBOARD CONTENT ===== -->
    <div class="page-content">
        <div class="container-fluid">
            <!-- Page Header -->
            <div class="page-header d-flex justify-content-between align-items-center">
                <div>
                    <h1 class="h3 mb-1">Blog List </h1>
                    <p class="text-muted small mb-0">Welcome back, Here's your overview</p>
                </div>

            </div>

            <!-- Stat Cards -->
            <div class="row g-4">
                <div class="col-12">
                    <table border="1" class="table table-border">

                        <tr>
                            <th>ID</th>
                            <th>Blog Image</th>
                            <th>Blog Date</th>
                            <th>Blog Name</th>
                            <th>Blog Description</th>
                            <th>Status</th>
                        </tr>
                        <% for ( var i=0;i<blog.length;i++){ %>
                            <tr>
                                <td>
                                    <%= i+1 %>
                                </td>
                                <td>
                                    <%= blog[i].blog_img %>
                                </td>
                                <td>
                                    <%= blog[i].blog_date %>
                                </td>
                                <td>
                                    <%= blog[i].blog_name %>
                                </td>
                                <td>
                                    <%= blog[i].blog_desc %>
                                </td>
                                <td>
                                    <a href="/admin/blog_delete/<%= blog[i].id %>">
                                        <button class="btn btn-danger">
                                            <i class="fa fa-trash"></i>
                                        </button>
                                    </a>
                                    <a href="/admin/blog_edit/<%= blog[i].id %>">
                                        <button class="btn btn-warning">
                                            <i class="fa fa-pencil"></i>
                                        </button>
                                    </a>
                            </tr>
                            <% } %>
                    </table>

                </div>
            </div>



        </div>
    </div>
    <%- include('footer.ejs'); %>